spring.application.name=backend
server.port=8081

# JWT Configuration
jwt.secret=SOSMascotasChileSecretKeyFor256BitHMACEncryptionBase64EncodedValue1234567890AB

# Database Configuration - SQLite (Development)
spring.datasource.url=jdbc:sqlite:sos_mascotas.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# PostgreSQL Configuration (Uncomment to use PostgreSQL for production/Docker)
# spring.datasource.url=jdbc:postgresql://localhost:5432/sos_mascotas
# spring.datasource.username=postgres
# spring.datasource.password=your_password
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL15Dialect
# spring.jpa.hibernate.ddl-auto=update
